# deps
node_modules/
.pnpm-store/

# builds
dist/
build/
.vite/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# database (local dev)
*.db
*.db-journal
*.sqlite
packages/db/prisma/dev.db*
packages/db/prisma/migrations/dev.db*

# uploaded media (local mock provider) — never commit student video
storage/
!storage/.gitkeep

# logs
*.log
npm-debug.log*
logs/

# editor / os
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# scratch
work/

# local session handoff notes — working state, not project docs
PROGRESS.md

# local PATH shim from the gh CLI install — machine-specific, not project code
powershell.cmd

# vercel local state
.vercel/

# claude code local state (machine-specific permissions)
.claude/settings.local.json
